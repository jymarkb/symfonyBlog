{% set statsImages = [
    'new1.svg',
    'new2.svg',
    'new3.svg',
    'new4.svg'
] %}

<section class="second-section flex flex-col items-center">
    <div class="wrapper w-full px-8 sm:px-24 p-16 flex flex-col items-center gap-4">
        <div class="flex items-center bg-white border px-4 py-2 rounded-full shadow">
            <span class="block h-3 w-3 rounded-full border-2 border-primaryTheme bg-teal-400 mr-1"></span> ✨ What Make This Blog Different?
        </div>

        <div class="stats-container rounded-2xl overflow-hidden grid grid-cols-1 lg:grid-cols-2 gap-4">
            {% for image in statsImages %}
                <div class="flex justify-center items-center">                
                    <img loading="lazy" src="{{ asset('dist/about/svg/' ~ image) }}" height="380" width="380" alt="image text">
                </div>
            {% endfor %}
        </div>
    </div>
</section>
